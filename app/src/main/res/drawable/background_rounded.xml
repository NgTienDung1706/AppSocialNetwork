<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Màu nền của background -->
    <solid android:color="@color/rd" />

    <!-- Bo tròn các góc -->
    <corners
        android:topRightRadius="16dp"
        />

    <!-- Đặt viền nếu cần -->
    <stroke
        android:color="@color/st"
        android:width="2dp" />
</shape>
